export const feeConfig = {
  // Fee charged when a user removes a trait slot (no registry price applies)
  removalFeeAlgo: 5,

  // Estimated per-transaction network fee (used for UI cost previews)
  estimatedTxFeeAlgo: 0.001,

  // Estimated transaction count for a trait-swap operation
  estimatedTxCountSwap: 3,

  // Estimated transaction count for a shop / loot-box purchase
  estimatedTxCountShop: 2,
};
